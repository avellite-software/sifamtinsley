{% comment %}
  Section: Text with Box
  - A two-column layout with a text block and a styled contact box.
  - Uses Flexbox to automatically stack on mobile devices.
  - All content is editable in the Theme Customizer.
{% endcomment %}

<style>
  .support-section-container {
    max-width: 1200px;
    margin: 60px auto;
    padding: 0 20px;
  }

  .support-flex-wrapper {
    display: flex;
    align-items: flex-start; /* Aligns items to the top */
    gap: 40px; /* Space between the two columns */
    flex-wrap: wrap; /* Allows columns to stack on mobile */
  }

  /* Left Column: Main Text */
  .support-main-text {
    flex: 1.5; /* Makes this column wider */
    min-width: 300px; /* Prevents it from getting too squished */
    color: #333;
    line-height: 1.7;
  }

  .support-main-text p {
    margin: 0 0 1em;
  }

  /* Right Column: Support Box */
  .support-box {
    flex: 1; /* Makes this column narrower */
    min-width: 300px;
    background-color: #fff;
    border-radius: 12px;
    padding: 40px;
    text-align: center;
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.08);
  }

  .support-box-heading {
    font-size: 32px;
    font-weight: 800;
    color: #1c1c1c;
    margin: 0 0 15px;
  }

  .support-box-subheading {
    font-size: 16px;
    line-height: 1.6;
    color: #555;
    margin: 0 0 30px;
  }

  .support-box-phone {
    font-size: 20px;
    font-weight: 600;
    color: #333;
    margin: 0 0 30px;
    display: block; /* Makes it a block to take up its own line */
  }

  .support-box-button {
    display: block;
    width: 100%;
    background-color: #c1151b;
    color: #fff;
    text-decoration: none;
    font-size: 18px;
    font-weight: 600;
    padding: 15px 20px;
    border-radius: 8px;
    transition: background-color 0.2s;
  }

  .support-box-button:hover {
    background-color: #a10f16;
  }

  /* Responsive stacking for smaller screens */
  @media (max-width: 768px) {
    .support-flex-wrapper {
      flex-direction: column; /* Stacks the columns vertically */
      align-items: stretch; /* Makes columns full width */
    }
  }
</style>

<div class="support-section-container">
  <div class="support-flex-wrapper">
    <!-- Left Column -->
    <div class="support-main-text">
      {{ section.settings.main_text }}
    </div>

    <!-- Right Column -->
    <div class="support-box">
      <h2 class="support-box-heading">{{ section.settings.box_heading }}</h2>
      <p class="support-box-subheading">{{ section.settings.box_subheading }}</p>
      <span class="support-box-phone">{{ section.settings.box_phone }}</span>
      {% if section.settings.button_label != blank and section.settings.button_link != blank %}
        <a href="{{ section.settings.button_link }}" class="support-box-button">
          {{ section.settings.button_label }}
        </a>
      {% endif %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Text with Box",
  "tag": "section",
  "settings": [
    {
      "type": "header",
      "content": "Left Column Content"
    },
    {
      "type": "richtext",
      "id": "main_text",
      "label": "Main Text",
      "default": "<p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed nec ex quam. Etiam non commodo magna. In egestas nisi at dignissim ultricies. Morbi vehicula eu arcu ut finibus.</p><p>Suspendisse nulla mauris, tempor eget vulputate at, egestas ac elit. Aenean enim mi, tristique sed sollicitudin eget, venenatis a metus.</p>"
    },
    {
      "type": "header",
      "content": "Right Column (Support Box)"
    },
    {
      "type": "text",
      "id": "box_heading",
      "label": "Heading",
      "default": "Sales & Support"
    },
    {
      "type": "text",
      "id": "box_subheading",
      "label": "Subheading",
      "default": "A dedicated support team to support new purchases, technical queries, troubleshooting, and delivery information."
    },
    {
      "type": "text",
      "id": "box_phone",
      "label": "Phone Number",
      "default": "Call 01376 335271"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button Label",
      "default": "Email Support"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link"
    }
  ],
  "presets": [
    {
      "name": "Text with Box"
    }
  ]
}
{% endschema %}
