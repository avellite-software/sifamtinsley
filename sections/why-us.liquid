{% comment %}
  Section: Why Us
  - A three-column layout featuring two content boxes and a large number highlight.
  - Uses Flexbox to stack responsively on smaller screens.
  - All content is editable via section settings and blocks.
{% endcomment %}

<style>
  .why-us-section-container {
    max-width: 1200px;
    margin: 60px auto;
    padding: 0 20px;
    /* FIX: Use the theme's default body font */
    font-family: {{ settings.font_body.family }}, {{ settings.font_body.fallback_families }};
    font-weight: {{ settings.font_body.weight }};
    font-style: {{ settings.font_body.style }};
  }

  /* FIX: Use the theme's default heading font for all headings in this section */
  .why-us-main-heading,
  .why-us-box .rte,
  .why-us-number-column .number-highlight {
    font-family: {{ settings.font_heading.family }}, {{ settings.font_heading.fallback_families }};
    font-weight: {{ settings.font_heading.weight }};
    font-style: {{ settings.font_heading.style }};
  }

  .why-us-main-heading {
    font-size: 28px;
    color: #1c1c1c;
    margin: 0 0 40px;
  }

  .why-us-flex-wrapper {
    display: flex;
    gap: 30px;
    align-items: center;
  }

  .why-us-box,
  .why-us-number-column {
    flex: 1;
    min-width: 280px;
    max-width: 550px;
  }

  .why-us-box {
    background-color: #fff;
    border-radius: 16px;
    padding: 100px 40px;
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.07);
  }

  .why-us-box .rte {
    font-size: 40px;
    line-height: 1.4;
    color: #1c1c1c;
  }
  .why-us-box .rte p {
    margin: 0;
  }

  .why-us-number-column {
    padding: 40px;
  }

  .why-us-number-column .number-highlight {
    font-size: 120px;
    line-height: 1;
    color: #1c1c1c;
    margin: 0 0 10px;
  }

  .why-us-number-column .number-text .rte {
    font-size: 16px;
    line-height: 1.6;
    color: #555;
  }
  .why-us-number-column .number-text .rte p {
    margin: 0;
  }

  @media (max-width: 990px) {
    .why-us-flex-wrapper {
      flex-direction: column;
      align-items: center;
      gap: 30px;
    }
    .why-us-number-column {
      padding: 20px;
    }
    .why-us-box .rte {
      font-size: 24px;
    }
  }
</style>

<div class="why-us-section-container">
  {% if section.settings.main_heading != blank %}
    <h2 class="why-us-main-heading">{{ section.settings.main_heading | escape }}</h2>
  {% endif %}

  <div class="why-us-flex-wrapper">
    {%- for block in section.blocks -%}
      <div {{ block.shopify_attributes }}>
        {%- case block.type -%}
          {%- when 'feature_box' -%}
            <div class="why-us-box">
              <div class="rte">{{ block.settings.content }}</div>
            </div>

          {%- when 'large_number' -%}
            <div class="why-us-number-column">
              <div class="number-highlight">{{ block.settings.number | escape }}</div>
              <div class="number-text">
                <div class="rte">{{ block.settings.text }}</div>
              </div>
            </div>
        {%- endcase -%}
      </div>
    {%- endfor -%}
  </div>
</div>

{% schema %}
{
  "name": "Why Us",
  "tag": "section",
  "settings": [
    {
      "type": "text",
      "id": "main_heading",
      "label": "Main Heading",
      "default": "Why Sifam Tinsley"
    }
  ],
  "blocks": [
    {
      "type": "feature_box",
      "name": "Feature Box",
      "limit": 2,
      "settings": [
        {
          "type": "richtext",
          "id": "content",
          "label": "Content",
          "default": "<p><strong>A Global Presence</strong> and Trusted Clients</p>"
        }
      ]
    },
    {
      "type": "large_number",
      "name": "Large Number Column",
      "limit": 1,
      "settings": [
        {
          "type": "text",
          "id": "number",
          "label": "Large Number",
          "default": "75"
        },
        {
          "type": "richtext",
          "id": "text",
          "label": "Text",
          "default": "<p><strong>years</strong> of industry presence and experience an unrivalled pedigree in the industry.</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Why Us",
      "blocks": [{ "type": "feature_box" }, { "type": "feature_box" }, { "type": "large_number" }]
    }
  ]
}
{% endschema %}
